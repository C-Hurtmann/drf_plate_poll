version: '3'
services:
  database:
    image: 'postgres:latest'
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: inforce
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin